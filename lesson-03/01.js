// Напишите функцию calculateAverage, которая принимает 2 числа и возвращает их среднее арифметическое
let a
function calculateAverage(num1, num2) {
    let sum = (num1 + num2) / 2;
    return sum;

}
a = calculateAverage(10,113)
console.log(a);
